- [round 7] Consent scope grant: spec §5.6/decision 17 say the consent screen always grants both scopes ["swap:read","swap:write"] unconditionally; the plan (T30) instead grants requested ∩ both (a client asking only swap:read gets a narrower token). Which behavior is authorised? (a) spec-literal always-both + test-only minting path for T32's read-only-token tests; (b) authorise per-request scope narrowing (re-author decision 17).
